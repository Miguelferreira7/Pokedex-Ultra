import 'package:flutter/material.dart';

class PokeDialog extends StatelessWidget {
  const PokeDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
